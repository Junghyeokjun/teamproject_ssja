<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="teamproject.ssja.mapper.ProductListMapper">

	<select id="getProductList" parameterType="teamproject.ssja.dto.product.ProductCondition" resultType="teamproject.ssja.dto.ProductDto">
    	


select * from (SELECT p.* ,ROW_NUMBER() OVER (ORDER BY #{conditionSelect}) AS rnum
    FROM product p where p_c_no = #{category}) where rnum between (#{pageNum} * #{amount} - 39) and (#{pageNum} * #{amount})
	</select>
</mapper>
